[project]
name = "echinus-link"
version = "0.2.0"
description = "Echinus radio link — LoRa wire format and SX1262 HAT driver, shared by Node and Hub"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/echinus_link"]
